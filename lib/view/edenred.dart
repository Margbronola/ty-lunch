import 'package:flutter/material.dart';

class EdenredPage extends StatefulWidget {
  const EdenredPage({super.key});

  @override
  State<EdenredPage> createState() => _EdenredPageState();
}

class _EdenredPageState extends State<EdenredPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
